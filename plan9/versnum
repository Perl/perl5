/define PERL_VERSION_MAJOR/ {revision = $3}
/define PERL_VERSION_MINOR/ {version = $3}
/define PERL_VERSION_PATCH/ {subvers = $3}
END {
if (subvers == 0) 
	printf "p9pvers = %d.%d\n", revision, version> "buildinfo";
else
	printf "p9pvers = %d.%d.%d\n" , revision, version, subvers> "buildinfo";
}
